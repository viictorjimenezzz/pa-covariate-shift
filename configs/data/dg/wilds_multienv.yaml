_target_: src.data.wilds_datamodules.WILDSDataModule

dataset_name: ???
dataset_dir: ${paths.data_dir}/dg/dg_datasets/wilds/
cache: true

# Configurations for the multienvironment
train_config: ???
val_config: ???
test_config: ???

# Collate function and transform depends on the model:
transform: ???

# This is for the dataloader, depends on the architecture of the model:
batch_size: ???
num_workers: 0
pin_memory: False